<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:FamilyFinance"
             x:Class="FamilyFinance.Views.AddIncomeOrExpensePageView"
             Title="Новий Дохід Або Витрата">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="Large" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Margin="20">
            <Label
                Text="Назва:"
                VerticalOptions="Center" 
                HorizontalOptions="Start" />
            <Entry
                Text="{Binding ChangeMoney.Name, Mode=TwoWay}"
                BackgroundColor="DarkGray"/>
            <Label
                Text="Розмір:"
                VerticalOptions="Center" 
                HorizontalOptions="Start" />
            <Entry
                Text="{Binding ChangeMoney.FinalSize, Mode=TwoWay}"
                BackgroundColor="DarkGray"
                Keyboard="Numeric"/>
            <StackLayout
                HeightRequest="70"
                Orientation="Horizontal">
                <Picker
                    FontSize="Large"
                    Title="Категорія"
                    ItemsSource="{Binding Categories}"
                    ItemDisplayBinding="{Binding Name}"
                    Margin="40,0,0,0" />
                <Picker
                    FontSize="Large"
                    Title="Валюта"
                    ItemsSource="{Binding Currencies}"
                    ItemDisplayBinding="{Binding Name}"
                    HorizontalOptions="EndAndExpand"
                    Margin="0,0,40,0"/>
            </StackLayout>
            <StackLayout
                Margin="0,50,0,0"
                Orientation="Horizontal">
                <StackLayout
                    HorizontalOptions="StartAndExpand"
                    Style="{StaticResource addStackLayout}">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer
                            Command="{Binding AddCommand}"
                            CommandParameter="I"/>
                    </StackLayout.GestureRecognizers>
                    <Image
                        Source="plus_icon_green"
                        Style="{StaticResource addImage}"/>
                    <Label
                        Text="Дохід"
                        TextColor="{x:Static local:Theme.PlusGreenColor}"
                        FontSize="Large"
                        Style="{StaticResource addLabel}"/>
                </StackLayout>
                <StackLayout
                    HorizontalOptions="EndAndExpand"
                    Style="{StaticResource addStackLayout}">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer
                            Command="{Binding AddCommand}"
                            CommandParameter="E"/>
                    </StackLayout.GestureRecognizers>
                    <Image
                        Source="minus_icon_red"
                        Style="{StaticResource addImage}"/>
                    <Label
                        Text="Витрата"
                        TextColor="{x:Static local:Theme.MinusRedColor}"
                        FontSize="Large"
                        Style="{StaticResource addLabel}"/>
                </StackLayout>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>